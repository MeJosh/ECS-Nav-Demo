# Use a Small Monorepo

The demo uses a small monorepo with separate client and server packages, and shared types factored out when they become useful. This keeps browser rendering, server-owned simulation logic, and cross-boundary contracts distinct while preserving a lightweight local development workflow.
